// Criar Banco de Dados
CREATE DATABASE (NOME)

// Ver todos os Banco de dados
SHOW DATABASE

// Deleta o banco de dados
DROP DATABASE (nome)

// Exportando banco de dados
mysqldump -u root (nome do banco) > (nome do arquivo)

// Utilizando banco de dados
USE (nome do banco)

// como utilizar  banco de dados sem tá nele
SELECT * FROM (nome do banco).(nome da tabela)